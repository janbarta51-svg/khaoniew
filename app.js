const translations = {
  "Úvod":"Home","Denní menu":"Lunch menu","Jídelní lístek":"Menu","Fotogalerie":"Gallery","Jak se k nám dostat?":"How to get here","Kontakty":"Contact",
  "Rodinné thajské bistro · Brno":"Family-run Thai bistro · Brno","Thajské jídlo Khaoniew Thai Bistro":"Thai food at Khaoniew Thai Bistro","Thajsko":"Thailand","na talíři.":"on your plate.",
  "Tradiční recepty, čerstvé suroviny a vyvážené chutě. Každé jídlo připravujeme poctivě a na objednávku.":"Traditional recipes, fresh ingredients and balanced flavours. Every dish is freshly prepared to order.",
  "Polední menu":"Lunch menu","Objednat: 775 003 044":"Order: 775 003 044","Domácí kuchyně.":"Home cooking.","Thajské srdce.":"Thai heart.",
  "Malá rodinná restaurace v Černých Polích. Tradiční receptury předávané z generace na generaci, kvalitní suroviny a vůně, které vás přenesou do Thajska.":"A small family restaurant in Černá Pole. Recipes passed down through generations, quality ingredients and aromas that take you straight to Thailand.",
  "Autentické thajské chutě":"Authentic Thai flavours","Čerstvě na objednávku":"Freshly made to order","Polední nabídka · 11:00–15:00":"Lunch offer · 11:00–15:00",
  "polévka + hlavní jídlo + dezert":"soup + main course + dessert","Dnes":"Today","Zítra":"Tomorrow","Tento týden":"This week","Polévka":"Soup","hlavní chod":"main course","Dezert":"Dessert",
  "Čísla u jídel označují alergeny. Pálivá jídla jsou označena symbolem 🌶.":"Numbers next to dishes indicate allergens. Spicy dishes are marked with 🌶.",
  "V neděli máme zavřeno.":"We are closed on Sundays.","O víkendu polední menu nepodáváme.":"Lunch menu is not served at weekends.","Menu pro tento den zatím není zveřejněné.":"The menu for this day has not been published yet.","Prohlédněte si nabídku na celý pracovní týden.":"See the full weekday menu.","Zobrazit tento týden":"View this week",
  "Stálá nabídka":"À la carte","Thajské kari, wok, nudle i lehké předkrmy. Přílohu si vyberete podle chuti.":"Thai curries, wok dishes, noodles and light starters. Choose your preferred side.",
  "Thajská jídla":"Thai dishes","Nudle a rýže":"Noodles & rice","Předkrmy a polévky":"Starters & soups",
  "Kuřecí maso, červené kari, bambus, houby, zelenina a kokosové mléko":"Chicken, red curry, bamboo, mushrooms, vegetables and coconut milk","Hovězí maso, zelené kari, bambus, houby, zelenina a kokosové mléko":"Beef, green curry, bamboo, mushrooms, vegetables and coconut milk","Kachní prsa, červené kari, bambus, houby, zelenina a kokosové mléko":"Duck breast, red curry, bamboo, mushrooms, vegetables and coconut milk","Papájový salát, chilli, grilované kuře a lepkavá rýže":"Papaya salad, chilli, grilled chicken and sticky rice",
  "Rýžové nudle, kuřecí, vepřové a hovězí maso, zelenina, tofu a arašídy":"Rice noodles, chicken, pork, beef, vegetables, tofu and peanuts","Rýžové nudle s krevetami, zeleninou, tofu a arašídy":"Rice noodles with shrimp, vegetables, tofu and peanuts","Rýžové nudle se zeleninou a tofu":"Rice noodles with vegetables and tofu","Restovaná jasmínová rýže s kuřecím masem a zeleninou":"Stir-fried jasmine rice with chicken and vegetables",
  "Thajské závitky se zeleninou, houbami, nudlemi a sladkokyselou omáčkou":"Thai rolls with vegetables, mushrooms, noodles and sweet-and-sour sauce","Čerstvé závitky s krevetami, zeleninou, mangem, nudlemi a arašídovou omáčkou":"Fresh rolls with shrimp, vegetables, mango, noodles and peanut sauce","Pikantní thajská polévka s krevetami a houbami":"Spicy Thai soup with shrimp and mushrooms","Pikantní thajská polévka s kuřecím masem a kokosovým mlékem":"Spicy Thai soup with chicken and coconut milk",
  "od 209 Kč":"from CZK 209","od 229 Kč":"from CZK 229","od 239 Kč":"from CZK 239","od 249 Kč":"from CZK 249","od 309 Kč":"from CZK 309",
  "Celou nabídku najdete také u rozvozových partnerů.":"The full menu is also available from our delivery partners.","Objednat online ↗":"Order online ↗","Ochutnejte očima":"A taste for the eyes","Prohlédněte si naše thajské speciality a příjemné venkovní posezení v Černých Polích.":"Discover our Thai specialities and relaxed outdoor seating in Černá Pole.","Naše bistro":"Our bistro",
  "„Chuť Thajska,":"“The taste of Thailand,","připravená jako doma.“":"prepared just like home.”","Jsme v Černých Polích, jen pár minut pěšky od zastávky Zemědělská.":"Find us in Černá Pole, just a few minutes' walk from the Zemědělská stop.",
  "Tramvají":"By tram","Linkou 7 nebo 9 na zastávku":"Take tram 7 or 9 to","Odtud je bistro přibližně 4 minuty pěšky.":"The bistro is about a 4-minute walk from there.","Pěšky nebo na kole":"On foot or by bike","Vchod i venkovní posezení najdete přímo v ulici Zemědělská. Kolo lze nechat u bistra.":"The entrance and outdoor seating are directly on Zemědělská Street. Bicycles can be left by the bistro.","Autem":"By car","Do navigace zadejte „Khaoniew Thai Bistro“ nebo Zemědělská 1693/38. Parkování hledejte v okolních ulicích.":"Enter “Khaoniew Thai Bistro” or Zemědělská 1693/38 in your navigation. Street parking is available nearby.","Spustit navigaci ↗":"Start navigation ↗",
  "Kde nás najdete":"Where to find us","Naplánovat trasu ↗":"Plan your route ↗","Kontakt":"Contact","Otevírací doba":"Opening hours","Pondělí":"Monday","Úterý":"Tuesday","Středa":"Wednesday","Čtvrtek":"Thursday","Pátek":"Friday","Sobota":"Saturday","Neděle":"Sunday","Zavřeno":"Closed","Rodinné Thai Bistro v Brně.":"Family-run Thai Bistro in Brno.","Nahoru ↑":"Back to top ↑",
  "Palačinky":"Pancakes","Ovoce":"Fruit","Buchta":"Cake","Rýžová polévka":"Rice soup","Kuřecí polévka":"Chicken soup","Zeleninová polévka":"Vegetable soup","Miso":"Miso",
  "s kuřecím masem a houbami":"with chicken and mushrooms","s banánem a karamelem":"with banana and caramel","s kuřecím masem a zeleninou":"with chicken and vegetables","s kokosovým mlékem a karamelem":"with coconut milk and caramel","s vejcem a bambusem":"with egg and bamboo","s vejcem a tofu":"with egg and tofu",
  "křupavá kuřecí prsa, Thai kari, zelenina, bambus, houby, kokosové mléko, rýže":"crispy chicken breast, Thai curry, vegetables, bamboo, mushrooms, coconut milk and rice","losos, chilli, bambus, cibule, thajská omáčka Příloha: Jasmínová rýže":"salmon, chilli, bamboo, onion and Thai sauce · served with jasmine rice","restované rýžové nudle s vepřovým masem, zeleninou a arašídy":"stir-fried rice noodles with pork, vegetables and peanuts","vepřové maso, rýžové nudle, zelenina, zálivka":"pork, rice noodles, vegetables and dressing",
  "kuřecí, vepřové, hovězí maso, chilli, citronová tráva, galangal, kešu oříšky, vejce":"chicken, pork and beef, chilli, lemongrass, galangal, cashews and egg","restované kuřecí maso, kešu oříšky, houby, zelenina, mix nudle":"stir-fried chicken, cashews, mushrooms, vegetables and mixed noodles","restované udon nudle, kuřecí maso, zelenina, sušené cibule":"stir-fried udon noodles, chicken, vegetables and crispy onion","hovězí maso, rýžový nudle, zelenina, arašídy, sušené cibule, zálivka":"beef, rice noodles, vegetables, peanuts, crispy onion and dressing",
  "kuřecí a vepřové maso špíz, arašídová omáčka, mix nudle":"chicken and pork skewers, peanut sauce and mixed noodles","krevety, tofu, thai kari, houby, zelenina, kokosové mléko Příloha: Rýže":"shrimp, tofu, Thai curry, mushrooms, vegetables and coconut milk · served with rice","restované rýžové nudle s kuřecím masem, zeleninou a arašídy":"stir-fried rice noodles with chicken, vegetables and peanuts","Thajský salát s skleněnými nudlemi, mangem, bylinkami, tofu a arašídy":"Thai glass noodle salad with mango, herbs, tofu and peanuts",
  "grilovaný losos, kešu oříšky, zelenina Příloha: Jasmínová rýže":"grilled salmon, cashews and vegetables · served with jasmine rice","kuřecí maso, brambory, mrkev, cibule, kokosové mléko, Thai kari, nudle Bún":"chicken, potatoes, carrot, onion, coconut milk, Thai curry and bún noodles","kachní prsa s nudlemi, shiitake houbami, sójovou omáčkou a bylinkami":"duck breast with noodles, shiitake mushrooms, soy sauce and herbs","restovaná jasmínová rýže s kuřecím masem a ananasem":"stir-fried jasmine rice with chicken and pineapple","křupavá kuřecí prsa, Thai kari, vejce, zelenina, kokosové mléko, rýže":"crispy chicken breast, Thai curry, egg, vegetables, coconut milk and rice","kachní prsa na medu a jasmínová rýže":"honey-glazed duck breast with jasmine rice","Letní závitky s krevetami, mangem, zeleninou, arašídová omáčka, mix nudle":"fresh rolls with shrimp, mango, vegetables, peanut sauce and mixed noodles","Příloha: Rýže":"served with rice"
};

const dayNames = [
  { cz:"Pondělí", en:"Monday", match:"PONDĚLÍ" },
  { cz:"Úterý", en:"Tuesday", match:"ÚTERÝ" },
  { cz:"Středa", en:"Wednesday", match:"STŘEDA" },
  { cz:"Čtvrtek", en:"Thursday", match:"ČTVRTEK" },
  { cz:"Pátek", en:"Friday", match:"PÁTEK" }
];

let language = localStorage.getItem("khaoniew-language") === "en" ? "en" : "cz";
let activeTab = "today";
let weeklyMenu = null;

function translate(text) {
  return language === "en" ? (translations[text] || text) : text;
}

function escapeHtml(text) {
  return String(text ?? "").replace(/[&<>'"]/g, character => ({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[character]);
}

function localDate(value) {
  const [year, month, day] = value.split("-").map(Number);
  return new Date(year, month - 1, day);
}

function addDays(date, count) {
  const result = new Date(date);
  result.setDate(result.getDate() + count);
  return result;
}

function dayDifference(date, reference) {
  const left = new Date(date.getFullYear(), date.getMonth(), date.getDate()).getTime();
  const right = new Date(reference.getFullYear(), reference.getMonth(), reference.getDate()).getTime();
  return Math.round((left - right) / 86400000);
}

function formatDate(date) {
  return new Intl.DateTimeFormat(language === "en" ? "en-GB" : "cs-CZ", { day:"numeric", month:"numeric" }).format(date);
}

function normalizeText(value) {
  return value.replace(/\r/g, "").replace(/\u00a0/g, " ").replace(/[ \t]+/g, " ").split("\n").map(line => line.replace(/^[•·–—-]\s*/, "").trim()).filter(Boolean).join("\n");
}

function extractAllergens(value) {
  const match = value.match(/(?:^|\s)((?:\d{1,2}\s*,\s*)*\d{1,2})\s*$/);
  if (!match || match.index === undefined) return { text:value.trim(), allergens:"" };
  return { text:value.slice(0, match.index).trim(), allergens:match[1].split(",").map(item => item.trim()).filter(Boolean).join(", ") };
}

function splitDish(value, simple) {
  const expression = simple ? /\s+(s|se|tradiční)\s+/i : /\s+(křupav(?:á|é|ý)|restovan(?:é|á|ý)|grilovan(?:ý|á|é)|pečen(?:é|á|ý)|smažen(?:é|á|ý)|kuřecí|vepřové|hovězí|kachní|losos|krevety|thajsk(?:ý|á|é)|letní|rýžové|udon|zelenin(?:a|ový|ová|ové))(?=\s|,|$)/i;
  const match = value.match(expression);
  if (!match || match.index === undefined) return { name:value.trim(), desc:"" };
  return { name:value.slice(0, match.index).trim(), desc:value.slice(match.index + 1).trim() };
}

function parseDish(code, value, simple = false) {
  const result = extractAllergens(value);
  const spicy = /🌶|\bchilli?\b/i.test(result.text);
  const clean = result.text.replace(/🌶️?/g, "").replace(/\s+/g, " ").trim();
  const dish = splitDish(clean, simple);
  return { code, name:dish.name, desc:dish.desc, allergens:result.allergens, spicy };
}

function mergeAllergens(first, second) {
  return [...new Set(`${first},${second}`.split(",").map(item => item.trim()).filter(Boolean))].join(", ");
}

function parseDay(lines, index) {
  const info = dayNames[index];
  const first = (lines[0] || "").replace(new RegExp(`^${info.match}\\s*`, "i"), "");
  const soupMatch = first.match(/Polévka\s*:\s*(.+)$/i);
  if (!soupMatch) return null;
  const meals = [];
  let current = null;
  let dessert = null;
  for (const line of lines.slice(1)) {
    const dessertMatch = line.match(/^Dezert\s*:\s*(.+)$/i);
    if (dessertMatch) {
      dessert = parseDish("Dezert", dessertMatch[1], true);
      current = null;
      continue;
    }
    const mealMatch = line.match(/^([A-ZÁČĎÉĚÍŇÓŘŠŤÚŮÝŽ]\d+)\s*[.:]?\s+(.+)$/i);
    if (mealMatch) {
      current = parseDish(mealMatch[1].toUpperCase(), mealMatch[2]);
      meals.push(current);
      continue;
    }
    if (current) {
      const continuation = extractAllergens(line);
      current.desc = `${current.desc}${/^Příloha\s*:/i.test(continuation.text) ? " · " : " "}${continuation.text}`.trim();
      current.allergens = mergeAllergens(current.allergens, continuation.allergens);
    }
  }
  if (!meals.length || !dessert) return null;
  return { day:info.cz, dayEn:info.en, soup:parseDish("Polévka", soupMatch[1], true), meals, dessert };
}

function priceFrom(text, pattern, fallback) {
  const match = text.match(pattern);
  return match ? Number(match[1]) : fallback;
}

function parseMenu(payload) {
  if (!payload || !/^\d{4}-\d{2}-\d{2}$/.test(payload.weekStart || "")) return null;
  if (typeof payload.menuText !== "string" || !payload.menuText.trim()) return Array.isArray(payload.days) ? payload : null;
  const text = normalizeText(payload.menuText);
  const lines = text.split("\n");
  const starts = dayNames.map(day => lines.findIndex(line => new RegExp(`^${day.match}(?:\\s|$)`, "i").test(line)));
  if (starts.some(index => index < 0)) return null;
  const days = dayNames.map((_, index) => parseDay(lines.slice(starts[index], index === dayNames.length - 1 ? lines.length : starts[index + 1]), index));
  if (days.some(day => !day)) return null;
  return {
    weekStart:payload.weekStart,
    prices:{
      set:priceFrom(text, /MENU\s*SET[\s\S]{0,80}?(\d{2,4})\s*Kč/i, 189),
      soup:priceFrom(text, /(?:^|\n)\s*Polévka\s*:\s*(\d{2,4})\s*Kč/im, 25),
      main:priceFrom(text, /Hlavní\s*chod\s*:\s*(\d{2,4})\s*Kč/i, 165),
      dessert:priceFrom(text, /(?:^|\n)\s*Dezert\s*:\s*(\d{2,4})\s*Kč/im, 20)
    },
    days
  };
}

function mealText(meal, field) {
  if (language === "en") {
    const englishField = field === "name" ? meal.nameEn : meal.descEn;
    return englishField?.trim() || translate(meal[field]);
  }
  return meal[field];
}

function dayCard(day, date, compact = false) {
  const mealRows = day.meals.map(meal => `<div class="mealRow"><span>${escapeHtml(meal.code)}</span><div><h4>${escapeHtml(mealText(meal, "name"))}${meal.spicy ? "<i>🌶</i>" : ""}${meal.veg ? "<em>veg</em>" : ""}</h4><p>${escapeHtml(mealText(meal, "desc"))}</p></div><small>${escapeHtml(meal.allergens)}</small></div>`).join("");
  return `<article class="weekDay${compact ? " compact" : ""}"><header><div><span>${escapeHtml(language === "en" ? (day.dayEn || translate(day.day)) : day.day)}</span><b>${formatDate(date)}</b></div><strong>11:00–15:00</strong></header><div class="soupLine"><span>${translate("Polévka")}</span><h4>${escapeHtml(mealText(day.soup, "name"))}${day.soup.spicy ? "<i>🌶</i>" : ""}</h4><p>${escapeHtml(mealText(day.soup, "desc"))}</p><small>${escapeHtml(day.soup.allergens)}</small></div><div class="mealRows">${mealRows}</div><div class="dessertLine"><span>${translate("Dezert")}</span><b>${escapeHtml(mealText(day.dessert, "name"))}</b><p>${escapeHtml(mealText(day.dessert, "desc"))}</p><small>${escapeHtml(day.dessert.allergens)}</small></div></article>`;
}

function renderMenu() {
  if (!weeklyMenu) return;
  const now = new Date();
  const monday = localDate(weeklyMenu.weekStart);
  const friday = addDays(monday, 4);
  const selectedDate = activeTab === "tomorrow" ? addDays(now, 1) : now;
  const selectedIndex = dayDifference(selectedDate, monday);
  document.querySelector('[data-tab="today"] small').textContent = formatDate(now);
  document.querySelector('[data-tab="tomorrow"] small').textContent = formatDate(addDays(now, 1));
  document.querySelector('[data-tab="week"] small').textContent = `${formatDate(monday)}–${formatDate(friday)}`;
  document.getElementById("setPrice").textContent = language === "en" ? `Menu set CZK ${weeklyMenu.prices.set}` : `Menu set ${weeklyMenu.prices.set} Kč`;
  document.getElementById("lunchPrices").innerHTML = `<span><b>${language === "en" ? `CZK ${weeklyMenu.prices.soup}` : `${weeklyMenu.prices.soup} Kč`}</b> ${translate("Polévka").toLowerCase()}</span><span><b>${language === "en" ? `CZK ${weeklyMenu.prices.main}` : `${weeklyMenu.prices.main} Kč`}</b> ${translate("hlavní chod")}</span><span><b>${language === "en" ? `CZK ${weeklyMenu.prices.dessert}` : `${weeklyMenu.prices.dessert} Kč`}</b> ${translate("Dezert").toLowerCase()}</span>`;
  const content = document.getElementById("menuContent");
  if (activeTab === "week") {
    content.innerHTML = `<div class="weekList">${weeklyMenu.days.map((day, index) => dayCard(day, addDays(monday, index), true)).join("")}</div>`;
    return;
  }
  if (selectedIndex >= 0 && selectedIndex < weeklyMenu.days.length) {
    content.innerHTML = dayCard(weeklyMenu.days[selectedIndex], selectedDate);
    return;
  }
  const message = selectedDate.getDay() === 0 ? "V neděli máme zavřeno." : selectedDate.getDay() === 6 ? "O víkendu polední menu nepodáváme." : "Menu pro tento den zatím není zveřejněné.";
  content.innerHTML = `<div class="closedMenu"><b>${translate(message)}</b><p>${translate("Prohlédněte si nabídku na celý pracovní týden.")}</p><button type="button" data-show-week>${translate("Zobrazit tento týden")}</button></div>`;
  content.querySelector("[data-show-week]").addEventListener("click", () => selectTab("week"));
}

function applyLanguage() {
  document.documentElement.lang = language === "en" ? "en" : "cs";
  document.querySelectorAll("[data-i18n]").forEach(element => {
    const original = element.dataset.i18n;
    element.textContent = translate(original);
  });
  document.querySelectorAll("[data-i18n-alt]").forEach(element => {
    element.alt = translate(element.dataset.i18nAlt);
  });
  document.querySelectorAll("[data-lang]").forEach(button => {
    const active = button.dataset.lang === language;
    button.classList.toggle("active", active);
    button.setAttribute("aria-pressed", String(active));
  });
  renderMenu();
}

function selectTab(tab) {
  activeTab = tab;
  document.querySelectorAll("[data-tab]").forEach(button => {
    const active = button.dataset.tab === tab;
    button.classList.toggle("active", active);
    button.setAttribute("aria-selected", String(active));
  });
  renderMenu();
}

document.querySelectorAll("[data-lang]").forEach(button => button.addEventListener("click", () => {
  language = button.dataset.lang;
  localStorage.setItem("khaoniew-language", language);
  applyLanguage();
}));

document.querySelectorAll("[data-tab]").forEach(button => button.addEventListener("click", () => selectTab(button.dataset.tab)));

const topbar = document.querySelector(".topbar");
const menuButton = document.querySelector(".menuButton");
menuButton.addEventListener("click", () => {
  const open = topbar.classList.toggle("open");
  menuButton.setAttribute("aria-expanded", String(open));
});
document.querySelectorAll(".topbar nav a").forEach(link => link.addEventListener("click", () => {
  topbar.classList.remove("open");
  menuButton.setAttribute("aria-expanded", "false");
}));

fetch(`weekly-menu.json?v=${Date.now()}`, { cache:"no-store" })
  .then(response => {
    if (!response.ok) throw new Error("Menu se nepodařilo načíst");
    return response.json();
  })
  .then(payload => {
    weeklyMenu = parseMenu(payload);
    if (!weeklyMenu) throw new Error("Menu má neplatný formát");
    applyLanguage();
  })
  .catch(() => {
    document.getElementById("menuContent").innerHTML = `<div class="closedMenu"><b>${language === "en" ? "The weekly menu is being updated." : "Týdenní menu právě aktualizujeme."}</b></div>`;
  });

applyLanguage();
